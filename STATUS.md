# Codex Universal Work Status

Last updated: 2026-09-12

`PLAN.md` is the source of remaining scope, ordering, complexity, and rationale;
`REVIEW-ARCHIVE.md` preserves completed and refuted findings. This file tracks
execution. Update an item's state when work starts or finishes, link its
validation evidence, and append a short entry to the work log.

Allowed states are `NOT STARTED`, `IN PROGRESS`, `DEFERRED`, `BLOCKED`, `DONE`,
and `DISMISSED`. Mark an item `DONE` only after the relevant focused checks pass;
security and image-boundary changes also require the applicable real-image or
runtime validation.

## Summary

| Tier | Items | Done | In progress | Remaining | Reason |
|---|---:|---:|---:|---:|---|
| Tier 1 | 10 | 10 | 0 | 0 | Issues 1 and 22 are rebuilt-image validated; all Tier 1 work is complete. |
| Tier 2 | 13 | 13 | 0 | 0 | Issues 47–49 are fixed; host-only and pinned-image-runtime fixtures pass at their intended boundaries. |
| Tier 3 | 23 | 21 | 0 | 2 | Issues 4, 20, 21, and 37 are complete; optional Issues 23 and 44 remain deferred by product decision. |
| Tier 4 | 3 | 0 | 0 | 0 | Three original claims were dismissed after targeted refutation. |

Evidence baseline: 27 dynamically confirmed, 17 source confirmed, 2 open, and
3 refuted items. There are 46 active planned items and 3 dismissed claims.

## Environment failures and prohibited behavior

| Date | Scope | Observed failure or prohibition | Impact | Next action | Reason |
|---|---|---|---|---|---|
| 2026-09-11 | First resumed container image | Docker CLI was absent; the packaged LSP proxy predated the frame-limit fix; the packaged worker observer was mode `0644`. | Host image enumeration and Issues 7/8 rebuilt-image validation could not run there. | Superseded by the later CUDA-profile session, but current-source image validation remains outstanding. | Prevent stale-image results from being recorded as validation of current source. |
| 2026-09-11 | First resumed CUDA exposure | `codex-bwrap-cuda`, `nvidia-smi`, NVIDIA variables, CUDA libraries, and `/dev/nvidia*` were absent. | That session was an older generic image, not a CUDA validation environment. | Superseded by the later CUDA-profile session. | Preserve the reason the first Cortex CUDA test was invalid. |
| 2026-09-11 | First resumed Cortex CUDA test | `clojure -M:mkl:cuda:test-cuda` exited 1 before application assertions because `jnicudart` was missing from `java.library.path`. | No Cortex correctness conclusion was possible from that run. | Superseded by the later sandboxed/elevated comparison. | Distinguish missing-image native loading from a Cortex test failure. |
| 2026-09-11 | Host smoke suite | Six runs passed all 18 Python tests, then stopped before later/image checks. The integrated `HOST-SMOKE-5-18-19-32-20260911` record again shows fake nREPL `AF_INET` socket creation failing with `EPERM`. | Full host-suite success and later launcher/image assertions cannot be claimed inside this sandbox. | Run `./tests/host-smoke.sh` on the Docker host. | The suite is host-native and its loopback fixture needs socket creation. |
| 2026-09-11 | Nested Bubblewrap fixture | Nested user/PID namespace setup was prohibited in the ordinary command sandbox; earlier asynchronous stderr reported a transient `/proc/PID/ns/ns` open failure. | That sandbox could not provide namespace-scoped supervisor evidence. | Superseded by the approved alternate execution boundary inside this container, where the Bubblewrap lifecycle passes without weakening isolation. | An environment denial is not authorization to remove security isolation. |
| 2026-09-11 | ACP relay fixture | Unix-socket creation was prohibited with `EPERM` in the ordinary command sandbox, so the delayed-relay-failure fixture could not execute there. | That sandbox could not validate Issue 41. | Superseded by an approved alternate execution boundary inside this container, where the fixture and positive relay probes pass. | The relay contract depends on real Unix and loopback sockets. |
| 2026-09-11 | External resources | OpenAI Cyber-access-gated resources are prohibited for this work and were not used. Initial ordinary-sandbox requests to public GitHub failed DNS resolution; approved read-only public GitHub metadata and release-asset downloads then succeeded. | Public release evidence is available, but only through the explicit network-approval boundary. | Continue using ordinary public upstream sources when necessary; never use the gated OpenAI resource. | Respect the user's access boundary while keeping dependency pins evidence-based. |
| 2026-09-11 | Second resumed CUDA image | The RTX 4090, CUDA 13.1 environment, driver 610.57.04, and CUDA libraries are visible, but installed workflow files still differ from current source and the packaged observer remains mode `0644`. | The CUDA profile is active, but Issues 7, 8, and 25 cannot be credited as rebuilt-current-image validation. | Commit/build current source so the launcher-selected tag contains the pending image changes; do not treat an older `latest` image as current. | Dirty builds intentionally do not update `latest`, so profile selection alone does not select the dirty build tag. |
| 2026-09-11 | Sandboxed Cortex CUDA test | `CORTEX-CUDA-20260911-R1` reached the GPU but returned 5 errors from `cudaHostAlloc`/`cudaErrorOperatingSystem` with an 8 MiB memlock limit: 9 tests, 13 assertions. | The ordinary command sandbox cannot validate pinned-host CUDA allocation. | Use the approved elevated CUDA path for workloads that require it; do not weaken the sandbox globally. | A sandbox resource denial must be separated from application correctness. |
| 2026-09-11 | Elevated Cortex CUDA comparison | The identical configured suite passed with 9 tests, 18 assertions, 0 failures, and 0 errors; stderr was empty. | Cortex native CUDA and the exposed GPU are working when run through the approved elevation boundary. | Retain this as application/GPU evidence; still test the new wrapper from a current image. | The controlled comparison confirms the sandboxed failure was environmental. |
| 2026-09-11 | Second host smoke run | `HOST-SMOKE-CUDA-20260911-R1` again passed all 18 Python tests, then fake nREPL AF_INET socket creation failed with `EPERM`; Docker/image checks did not execute. | Full host-suite or image-runtime success still cannot be claimed from inside this sandbox. | Run `./tests/host-smoke.sh` on the Docker host after building the committed current image. | The host-native loopback fixture requires socket creation prohibited here. |
| 2026-09-11 | Post-commit rebuild selection | The checkout is at `2594e22`, but untracked `review.md` made the rebuild dirty; the running image contains a workflow tree matching `34b3fdf` and older per-file versions rather than current source. | `latest` correctly remained on the old image, so Issues 8 and 25 are still not image-validated. | Build from a clean detached worktree, or launch the generated dirty version explicitly with `--image-version`. | Untracked files count as dirty and the protected `latest` alias must not move. |
| 2026-09-11 | Current rebuilt CUDA image | Installed entrypoints, CUDA wrapper, LSP proxy, probe configuration, installer, and observer matched that working tree; executable modes were `0755`. The sandbox exposed only minimal standard and NVIDIA character devices, denied `setns`, and prohibited both Unix and IP socket creation there. | Issues 7, 8, and 26 were image/runtime validated; socket-dependent work required a different execution boundary. | Socket behavior was subsequently validated through approved alternate-boundary probes inside this container; the newly changed Issue 1 files still require another rebuild. | Distinguish current-image proof from checks a particular command sandbox cannot execute. |
| 2026-09-11 | Current CUDA diagnostic path | `CORTEX-CUDA-HINT-CURRENT-20260911` exited 5 with 9 tests, 13 assertions, and 5 `cudaHostAlloc`/304 errors; the outer rebuilt wrapper appended the explicit approved-elevation note after the observed command exited. | Issue 25 is dynamically validated. The wrapper-added note is visible in the outer tool result but intentionally is not part of the observer's child stdout/stderr logs. | None. | Preserve the child exit/output while giving an actionable sandbox-specific diagnostic. |
| 2026-09-11 | Unix-socket policy comparison | The normal command sandbox allows `AF_UNIX` creation and `socketpair` but returns `EPERM` from pathname/abstract bind; `AF_INET`/`AF_INET6` creation also returns `EPERM`. The repository AppArmor/seccomp policy already permits Unix sockets. | Local relay fixtures cannot bind in ordinary tool commands, although separately managed/elevated processes can. | Permit narrowly scoped pathname Unix bind in the outer execution policy; keep IP networking restricted. | Local IPC should not require weakening the repository's network or privilege boundary. |
| 2026-09-11 | Cortex Unix-socket cleanup probe | The cleanup eval used an invalid one-argument Java `Files/deleteIfExists` invocation and returned a compiler exception. | The probe cleanup form failed, but stopping the managed runtime was confirmed and removed the socket path. | Use the correct Java varargs form in any durable probe; do not treat this as a Cortex failure. | Preserve probe-harness failures separately from product behavior. |
| 2026-09-11 | Elevated no-image host smoke | Socket-dependent Clojure lifecycle checks advanced successfully, then the suite stopped because its CUDA assertion matched legitimate per-device `/dev/nvidia*` binds as though all of `/dev` were exposed. | This exposed Issue 47; it was a false test failure, not broad device exposure. | Resolved by exact whole-device argument matching and a guarded no-device fixture; the suite now passes. | A security assertion must distinguish the required hardened layout from the insecure layout. |
| 2026-09-11 | ACP delayed-failure smoke fixture | Once Unix bind was permitted, the original fixture's `/tmp/.../idea.sock` was rejected by the production `/run/codex-idea-mcp/*.sock` validation before relay startup. A production-path Bubblewrap probe then passed both delayed-failure and positive relay cases. | This exposed Issue 48 in the checked-in fixture while confirming production behavior. | Resolved by mounting the disposable fixture at `/run/codex-idea-mcp` with Bubblewrap; the fixture now reaches delayed relay failure and passes. | Tests must exercise the production guard and relay lifecycle in the intended order. |
| 2026-09-11 | Networkless service `/proc` probe | Replacing the existing minimal `/proc` arrangement with Bubblewrap `--proc /proc` failed with `Operation not permitted` in this container. | A conventional procfs mount cannot be used for the isolated service here. | Retain the tested tmpfs `/proc/self/exe` compatibility boundary with `--unshare-net`. | Network isolation must not discard the existing nested-sandbox compatibility constraint. |
| 2026-09-11 | Initial Unix nREPL integration | The first nonblocking Java NIO evaluation decoded an invalid zero byte after a zero-length channel read. | Unix transport startup worked, but evaluation initially failed. | Fixed by flipping the empty read buffer before selector waiting; lifecycle and multi-value evaluations now pass. | Record an implementation failure separately from the final transport evidence. |
| 2026-09-11 | New namespace smoke fixture | Two first runs stopped on test wiring errors: an unbound helper variable, then a non-production FIFO name. | The new acceptance path had not executed on those runs. | Moved helper initialization before use and matched `service-control.fifo`; the complete suite subsequently passed. | Failed fixtures are not evidence about product behavior until their setup matches production. |
| 2026-09-11 | Production-path ACP fixture | The first Bubblewrap-mounted run reached the strict relay path but failed shell redirection with `/dev/null: Permission denied` before testing relay readiness. | That run did not validate the intended delayed-failure behavior. | Added a fresh Bubblewrap `--dev /dev`; the fixture and complete suite subsequently passed. | A fixture sandbox must provide its own usable device filesystem without binding the host's complete `/dev`. |
| 2026-09-11 | Pre-rebuild Issue 1 image boundary | The prior running image predated the new entrypoint and Unix-transport files. | Issue 1 could not then be marked `DONE` despite passing source and host Bubblewrap tests. | Superseded: installed hashes now match and the rebuilt Cortex lifecycle passes. | Do not report source-mounted components as installed-image validation. |
| 2026-09-11 | Rebuilt-image Docker inspection | Docker CLI is absent inside the rebuilt container. | Docker-driven image enumeration and the image-backed branch of the host suite cannot run from this session. | Use live installed-file hashes, kernel/runtime checks, and Cortex lifecycle evidence here; retain Docker-host smoke for the next host-side validation. | Distinguish a live rebuilt-container acceptance from Docker-daemon-driven host coverage. |
| 2026-09-11 | `TOOL-INSTALLER-SOURCE-20260911` | The finalized no-image suite passed its 13 test cases and all new installer/config/updater assertions, then fake nREPL startup failed because the outer sandbox prohibited `AF_INET` socket creation with `EPERM`. | Later host checks and image-backed checks cannot be credited from this run. | Rebuild and run `./tests/host-smoke.sh` from the Docker host. | Preserve the exact boundary between passing source checks and an environment-prohibited fixture. |
| 2026-09-11 | Installer linting | `shellcheck` is not installed in this container. | ShellCheck diagnostics cannot be claimed for the new installer/updater scripts. | Rely on `bash -n`, focused source assertions, updater execution, and rebuilt-image smoke; run ShellCheck later if available. | An unavailable optional validator must not be reported as passed. |
| 2026-09-11 | Rebuilt `c84db35` CUDA image | All configured Clojure/native tools except Leiningen report the pinned versions; Node 24, Temurin 25, codex-acp `1.11.0`, and agent-lsp `0.19.1` match. Installed entrypoints, workflow, LSP files, and CUDA wrapper match source. Both repository fingerprints and `signed-by` paths pass. Leiningen's installed launcher/JAR hashes match the 2.13.0 pins and the JAR manifest is `2.13.0`, but the authenticated upstream launcher exports `2.12.1-SNAPSHOT`, so `lein version` reports that stale value. | Issue 22 cannot yet be marked done; this is not a stale-image or checksum failure. The generic image was not independently inspected here. | Source now normalizes the authenticated launcher metadata and asserts its runtime report; rebuild and retest. | Installed artifact identity and runtime-reported identity should agree. |
| 2026-09-11 | Rebuild-verification probe corrections | The first toolchain probe incorrectly compared pinned ASCII-key hashes with dearmored keyring bytes and remains `running` with liveness `not-visible-or-exited`; cleanup could not see its PID. Two corrected key probes exited 2 from quoting mistakes, and a third exited 0 without enforcing its PASS output; final `IMAGE-TOOLCHAIN-KEYS-R5` correctly enforced all four assertions and exited 0. The first Cortex version form used an invalid API and was corrected once. A direct `--proc /proc` Bubblewrap probe was denied and the documented minimal `/proc/self/exe` form passed. An unwrapped `setns` call returned ordinary `EBADF`; the installed service-child filter correctly returns `EPERM`. | Failed or wrongly scoped probes are not product evidence and the original observer record cannot be claimed complete. | Retain the final enforced key result, corrected Cortex lifecycle, minimal-proc CUDA result, and wrapped `setns` result; preserve the unresolved observer liveness state. | Separate harness mistakes and execution-boundary denials from actual rebuilt-image behavior. |
| 2026-09-11 | Post-amend rebuilt-image smoke retest | `REBUILT-HOST-SMOKE-20260911` passed all 13 embedded tests, then the ordinary command sandbox denied the Unix-socket fixture with `EPERM`. Approved-boundary `REBUILT-HOST-SMOKE-R2-20260911` subsequently passed all 25 named checks and 13 tests. A redundant parent attempt to create the already-completed R2 observer ID exited 2 without rerunning or changing its evidence. | The first failure and duplicate-ID rejection are execution-harness results, not product failures; Docker/image checks were explicitly skipped in both runs. | Retain R2 as the local-only regression result and run image-backed smoke from a Docker host when desired. | Socket-dependent local checks require their approved boundary, and observer-ID reuse must not be mistaken for a test failure. |
| 2026-09-11 | External host smoke logs | `test.txt` repeated one load-time failure 14 times because the Docker machine's Babashka could not resolve `java.nio.channels.SelectionKey`; none of the intended helper behaviors ran. `test-sync.txt` stopped intentionally after detecting the running `codex-codex-universal` container. | The Clojure failure was a test/runtime-boundary defect, while the snapshot result was the required live-container safety guard. | Resolved by Issue 49; stop Codex containers before separately validating synchronization, or use `CODEX_TEST_SKIP_SYNC=1` for a reduced run. | Container-only helper behavior should use the pinned image runtime, while state replacement must remain prohibited during a live session. |
| 2026-09-11 | Issue 49 validation harness | The first post-extraction host-only run still used the ordinary command sandbox and stopped at the known LSP Unix-bind `EPERM`; approved-boundary R2 passed. The Clojure fixture observer retained its durable logs but did not create the requested convenience copies under `/tmp`. | Neither harness discrepancy is a product failure; the durable observer evidence and approved-boundary result are complete. | Use the R2 host result and durable `IMAGE-CLOJURE-FIXTURE-20260911` observer logs. | Preserve the exact boundary and evidence locations instead of overstating a failed or missing record. |

## Item tracker

| ID | Tier | State | Evidence | Next action | Reason |
|---:|---:|---|---|---|---|
| 1 | 1 | DONE | Installed entrypoint/helper/supervisor hashes match staged source. Cortex `cpu-dev` reports reachable Unix transport, retains a sentinel across separate evaluations, exposes only `lo`/`127.0.0.1` inside the JVM, preserves the same runtime PID, stops with confirmed termination, and removes the socket. | Run Docker-host image-backed smoke when a daemon is available. | The rebuilt runtime closes the network-approval bypass while preserving managed cross-command REPL access. |
| 2 | 1 | DONE | Launcher now requires `codex-universal`; focused disposable probes reject `unconfined`, `docker-default`, and arbitrary overrides. | Recheck in the rebuilt image-backed host suite. | Confirmed sandbox-profile substitution is closed at argument construction. |
| 7 | 1 | DONE | The rebuilt sandbox has a fresh minimal `/dev` containing the required NVIDIA character devices and no `/dev/dri`; the installed wrapper matches source. | None. | Prevent broad device exposure while retaining CUDA access. |
| 10 | 1 | DONE | Fake-Docker security PASS assertion was removed; direct checks in the resumed real container returned `CapEff=0`, `NoNewPrivs=1`, and `codex-universal (enforce)`. | Let the rebuilt-image host suite exercise the durable assertions when Docker is available. | Kernel properties were verified in a real container instead of inferred from argv. |
| 12 | 1 | DONE | Final session argv now contains `--pull=never`; focused probe passes. | Recheck in the rebuilt host suite. | Prevent unintended registry access. |
| 16 | 1 | DONE | Git URL userinfo is removed before `IMAGE_SOURCE`; dummy-credential probe passes. | Recheck build fixture after rebuild if desired. | Prevent credential persistence. |
| 22 | 1 | DONE | The rebuilt CUDA image reports `Leiningen 2.13.0`; its launcher exports `2.13.0`, the installed JAR manifest identifies Leiningen `2.13.0`, and the JAR matches its pinned SHA-256. All other configured tools, npm pins, repository fingerprints, `signed-by` paths, and installed workflow files were already verified. Approved-boundary `REBUILT-HOST-SMOKE-R2-20260911` passes 25 named checks plus 13 embedded tests. | Run Docker-host image-backed smoke when a daemon is available. | Reduce supply-chain drift while keeping routine updates to one command and two reviewable config files. |
| 27 | 1 | DONE | Proxy now bounds headers/bodies and handles malformed, missing, non-ASCII, and oversized frames without traceback; focused probes pass. | Recheck in the rebuilt image. | Prevent protocol-input crashes. |
| 42 | 1 | DONE | Installer now requires `codex-universal (enforce)`; the resumed container's live AppArmor label is exactly `codex-universal (enforce)`. | None. | Host enforcement is confirmed by the running container's kernel label. |
| 43 | 1 | DONE | Dirty build inputs suffix both derived and explicit versions and print a warning when `TAG_LATEST=1` updates the convenience alias; `TAG_LATEST=0` retains it, and unrelated files do not dirty the image. | Recheck the dirty warning and `latest` alias on the next host build. | Make publication risk visible while preserving the requested convenient default. |
| 8 | 2 | DONE | The rebuilt image's packaged observer matches source and is executable at mode `0755`; related installed scripts are also `0755`. | None. | Keep the documented inspection command runnable from the image. |
| 11 | 2 | DONE | Launcher now resolves the primary group with `id -g`; the smoke source check rejects `GROUPS[0]`. | Recheck launcher argv on the Docker host. | Match primary group ownership independently of supplementary-group ordering. |
| 17 | 2 | DONE | The build help and README now require a Git checkout even with explicit image names; a non-Git fixture verifies the provenance error. | None. | Preserve mandatory immutable revision/source metadata without a misleading error contract. |
| 24 | 2 | DONE | Production entrypoint uses the immutable image workflow path; installer overrides require a test flag plus canonical source and target paths under `/tmp`. | Recheck the image-default install after rebuild. | Prevent a caller from replacing managed workflow content through production configuration. |
| 25 | 2 | DONE | The rebuilt wrapper preserved the Cortex suite's exit 5 and full 9-test/13-assertion output, detected `cudaErrorOperatingSystem`, and appended the explicit approved-elevation note. | None. | Provide a deterministic actionable diagnosis without masking the command result. |
| 26 | 2 | DONE | In the rebuilt image, `setns(-1, 0)` receives `EPERM`, while `/bin/true` and ordinary Python thread creation succeed. | None. | Prevent unnecessary namespace re-entry without breaking normal child behavior. |
| 28 | 2 | DONE | Local policy/source audit removed broad `execute_command` and ambiguous `suggest_fixes`; scoped semantic edits remain gated by `default_tools_approval_mode="writes"` and human review. | Reconfirm tool availability and approval prompts in a rebuilt terminal session. | Preserve least privilege without removing intended refactor/edit workflows. |
| 41 | 2 | DONE | At `/run/codex-idea-mcp/*.sock`, delayed `socat` failure stopped ACP with status 1; the positive real-`socat` path exchanged `pong:relay`, and `socat` exited on parent-death signal 15. | Repair the separate checked-in fixture path under Issue 48. | The production relay behavior is dynamically confirmed outside the restrictive command sandbox. |
| 47 | 2 | DONE | The smoke assertion now rejects only the exact `--dev-bind /dev /dev` pair, permits required NVIDIA device pairs, and the CUDA no-device fixture uses a narrowly gated test-only mode. The approved-boundary no-image suite passes inside this container. | Recheck with image checks enabled after rebuild on the Docker host. | Preserve the hardened device boundary without a false failure on legitimate CUDA nodes. |
| 48 | 2 | DONE | Bubblewrap mounts the disposable socket fixture at production's `/run/codex-idea-mcp` path; delayed relay failure now passes while the ACP entrypoint retains its strict original path guard. | None. | Exercise delayed relay failure without weakening the production path guard. |
| 49 | 2 | DONE | The runtime fixture now executes only from `smoke_image`. With pinned Babashka `1.13.220`, `IMAGE-CLOJURE-FIXTURE-20260911` passes 13 network-context tests plus the full helper lifecycle. Approved-boundary host-only R2 passes 20 checks without invoking `bb`. | Run the complete image-enabled host suite outside this container. | Keep host launcher/sync validation independent of a container-only runtime while testing the helper against its shipped dependency. |
| 45 | 2 | DONE | Project-first `PROJECT --set profile|clojure-mcp VALUE` replaces the two leading option forms; focused probes cover both keys, preserved companion values, removed-form rejection, unknown keys, and missing values. | Recheck through the complete host suite. | Make repeated project configuration follow a conventional and extensible command shape. |
| 46 | 2 | DONE | `--image-version VERSION` overrides `CODEX_IMAGE_TAG` for one terminal launch; validation rejects malformed or duplicate versions and `--sessions` combinations. | Recheck through the complete host suite. | Select a known immutable build without changing persistent project configuration. |
| 4 | 3 | DONE | Forced restore skips corrupt live-state validation but still verifies the selected archive, restored hash, and incoming SQLite state before replacement. | None. | Preserve a strict recovery hatch when the local Codex instance is corrupt. |
| 5 | 3 | DONE | A focused fixture proves normal pull rejects incomplete generation 4 while `--force 1` restores the complete older snapshot and baselines the remote head. | None. | Keep recovery available while the provider is still transferring the newest snapshot. |
| 9 | 3 | DONE | `AGENTS.md` now describes portable fixed-identity images and runtime UID/GID replacement consistently with README. | None. | Keep contributor guidance accurate. |
| 13 | 3 | DONE | The launcher probes the shared lock nonblockingly, reports `Waiting for Codex state handoff lock` with its path on contention, then preserves indefinite waiting. | None. | Distinguish expected synchronization contention from a hung launch. |
| 14 | 3 | DONE | The shared handoff lock is acquired before `--sessions`/`--resume` read `state_5.sqlite`; a held-lock probe proves a row created during contention is visible only after release. | Recheck through the complete host suite. | Prevent pull from replacing session state during lookup or resume resolution. |
| 18 | 3 | DONE | `codex-push --help` exits successfully and unknown options fail before creating sync, state, or lock paths; the installed-command fixture is nonmutating. | None. | Prevent accidental snapshots. |
| 19 | 3 | DONE | `codex-sync-lib` supplies strict metadata/archive/checksum validation to both installed commands; both reject the malformed format fixture. | None. | Prevent validation drift. |
| 20 | 3 | DONE | Install failure now distinguishes successful rollback from failed rollback and reports the exact retained backup path; both outcomes have deterministic fixtures. | None. | Keep state ownership and manual recovery evidence explicit after restore failure. |
| 21 | 3 | DONE | Nine focused sync groups now cover argument non-mutation, lock contention, incomplete heads, forward/divergent recovery, metadata, corruption, real WAL/SHM state, bad archives/hash mismatch, and both rollback outcomes. | None. | Verify failure behavior at the snapshot replacement boundary. |
| 23 | 3 | DEFERRED | The shared Dockerfile blocks currently match; package presence does not establish that broad development tooling is unnecessary. | Revisit only if the Dockerfiles grow substantially or actual generic/CUDA drift appears. | Avoid a low-value refactor and speculative compatibility regressions. |
| 29 | 3 | DONE | Probe guidance now requires the exact helper and private Unix transport, reuse of the existing service, preservation of state/workdir, and no reconstructed Bubblewrap or duplicate nREPL. | None. | Make worker behavior consistent with the managed skill boundary. |
| 30 | 3 | DONE | The alternate nREPL recipe now warns that it bypasses supervision, private Unix transport, and the networkless namespace, and limits itself to installations without the skill. | None. | Make the lost isolation explicit before users copy the command. |
| 31 | 3 | DONE | `AGENTS.md` and `CONTRIBUTING.md` now use the host suite as the canonical command and distinguish always-run checks from local-image runtime coverage. | None. | Prevent no-image validation from being reported as runtime-hardening proof. |
| 32 | 3 | DONE | Doctor logic is a sourced companion, snapshot integration is independently runnable, and detailed IntelliJ guidance moved to `docs/intellij.md`; installation and relocation coverage preserves entry points. | Continue only with separately justified cohesive extractions. | Reduce monolith review surface without a behavior-changing rewrite. |
| 33 | 3 | DONE | README documents standalone full clones as the supported workaround; a real linked-worktree fixture asserts the explicit registration rejection. | Revisit only with a design that does not expose parent-repository metadata. | Pointer metadata lies outside the project-only mount and cannot be enabled as a parser-only change. |
| 34 | 3 | DONE | The snapshot lock comment now names `run-codex` instead of the obsolete `run-codex.sh`. | None. | Keep lock-coordination guidance tied to the actual command. |
| 35 | 3 | DONE | Push success and handoff output now uses synchronized-generation and configured-provider wording; a source assertion rejects the old messages. | None. | Match every supported sync provider rather than the default directory's provider. |
| 36 | 3 | DONE | Recovery docs state that `--list` is lock-free and an in-flight provider transfer can temporarily appear `INVALID`. | None. | Prevent a partial sync observation from being mistaken for durable corruption. |
| 37 | 3 | DONE | Current Codex state uses WAL-mode `*.sqlite` databases with WAL/SHM sidecars. Validation also discovers valid non-`.sqlite` databases and runs against private main/journal/WAL/SHM copies, preserving the exact whole-state hash. | None. | Validate complete logical databases without checkpointing or mutating archived state. |
| 38 | 3 | DONE | Launcher help now lists every supported user-facing environment variable, including compatibility and identity/workflow controls. | None. | Improve configuration discoverability without requiring source inspection. |
| 39 | 3 | DONE | Every launcher/push/pull `nullglob` enable now restores the caller's prior disabled state immediately after glob expansion. | None. | Reduce hidden coupling between later shell functions. |
| 40 | 3 | DONE | `.local-fixtures/` is now ignored by the tracked repository rules; the intentional `.env.example` exception remains available for a future example file. | None. | Prevent accidental local-artifact commits. |
| 44 | 3 | DEFERRED | Architecture audit found global opaque state cannot be safely filtered. | Revisit isolated per-project/sync-group roots with the other snapshot synchronization work. | Enable safe cross-machine project concurrency after higher-priority general work. |
| 3 | 4 | DISMISSED | GNU tar and `codex-pull` rejected tested traversal and symlink escapes. | Optionally add regression fixtures only. | Original vulnerability was not reproduced. |
| 6 | 4 | DISMISSED | Lock path spellings resolved to the same inode; relative paths are rejected. | Optionally split out `lsof` error handling. | Original split-lock claim was not reproduced. |
| 15 | 4 | DISMISSED | `/run` is already rejected; mounting `/tmp/PROJECT` targets a child path and no relay or tmpfs escape was demonstrated. The ignored, untracked Cortex checkout is unrelated to this claim. | Optionally reject the exact `/tmp` project root as trivial defense in depth. | Retain only the narrow plausible mount-collision case instead of the review's broad restriction. |

## Work log

| Date | Change | Validation | Reason |
|---|---|---|---|
| 2026-09-12 | Completed Issues 4, 20, 21, and 37; explicitly retained Issues 23 and 44 as deferred product decisions. Forced pull now bypasses corrupt live validation, rollback outcomes are actionable, SQLite discovery is suffix-tolerant, and integrity checks use private copies with real sidecars. | `SNAPSHOT-LOCK-CONTENTION-20260912` passed all nine focused sync groups. The canonical `./tests/host-smoke.sh` then passed every host and sync check in 15 seconds; Docker image-runtime checks were explicitly skipped because no daemon is available. | Restore recovery without weakening incoming validation, make failed installation recoverable, and prevent integrity checks from mutating hashed WAL state. |
| 2026-09-11 | Completed Issue 49 by moving Clojure-helper runtime smoke coverage from host Babashka into each image check. | Pinned-runtime `IMAGE-CLOJURE-FIXTURE-20260911` passed 13 tests and the complete lifecycle fixture. Approved-boundary `REFACTORED-HOST-SMOKE-R2-20260911` passed 20 host checks with intentional sync/image skips and no host `bb` invocation. | Test container-only behavior against the shipped runtime while keeping launcher and synchronization validation host-native. |
| 2026-09-11 | Split the live plan from the historical review ledger. | `PLAN.md` now contains exactly the six deferred items; `REVIEW-ARCHIVE.md` preserves all 39 completed items and three refuted claims with their IDs, evidence labels, complexity, and reasons. | Keep the actionable backlog scannable without discarding review history. |
| 2026-09-11 | Verified the post-amend rebuilt CUDA image and completed Issue 22. | `lein version` now reports `2.13.0`; installed launcher metadata and the JAR manifest agree, and the JAR retains its pinned checksum. Ordinary-boundary smoke stopped at the known Unix-bind `EPERM`; approved-boundary `REBUILT-HOST-SMOKE-R2-20260911` passed 25 named checks and 13 embedded tests, with image checks explicitly skipped. | Close the last Tier 1 item using installed-runtime evidence while preserving the Docker-host coverage boundary. |
| 2026-09-11 | Corrected the authenticated Leiningen 2.13.0 launcher's stale embedded version metadata. | The raw launcher first matches its pinned SHA-256; the installer requires exactly one version declaration, rewrites it to the configured release, and rejects a mismatched `lein version` result. Applying the same transformation to the installed pinned launcher reports `Leiningen 2.13.0` with the installed 2.13.0 JAR. Inside this container, `LEIN-NORMALIZE-SOURCE-20260911` passed 25 named no-image checks and 13 embedded tests; Docker image checks were explicitly skipped. | Preserve upstream input authentication while making runtime identity accurate and future launcher-shape drift fail closed. |
| 2026-09-11 | Verified the rebuilt `c84db35` CUDA container and kept Issue 22 open for a Leiningen metadata defect. | Tool versions, source/installed-file identity, repository fingerprints, signed sources, non-root UID/GID, zero capabilities, `NoNewPrivs`, live `codex-universal (enforce)`, CUDA 13.1/RTX 4090 exposure, minimal NVIDIA-only nested `/dev`, wrapped `setns` denial, and the Cortex Unix-REPL lifecycle pass. The Cortex socket was `0600`, its JVM saw only `lo`/`127.0.0.1`, state survived separate evaluations, and stop removed the socket. Exact Leiningen artifacts match pins but the upstream launcher reports `2.12.1-SNAPSHOT` while its JAR manifest is `2.13.0`. | Distinguish a successfully rebuilt image from the one remaining runtime identity inconsistency before closing the supply-chain item. |
| 2026-09-11 | Hardened all remote installer inputs in Issue 22 and added a common updater. | Public release metadata matches the pins; all four native archives passed checksum, extraction, and executable version probes. The updater advanced Leiningen to `2.13.0` and its finalized rerun was idempotent. NodeSource and Adoptium public key hashes/fingerprints match the configs. Inside this container, approved-boundary run `TOOL-INSTALLER-ELEVATED-20260911` passed 25 named checks and 13 embedded tests; image checks were deliberately skipped. Docker-host and rebuilt-image checks remain pending. | Replace mutable installer execution with auditable inputs while keeping updates to one command and two cache-separated config files. |
| 2026-09-11 | Partially implemented Issue 22 by pinning locally proven ACP and agent-lsp versions. | Installed manifests report codex-acp `1.11.0` and agent-lsp `0.19.1`; both Dockerfiles and build-wrapper defaults agree, help/docs match, and the complete approved-boundary no-image suite passes inside this container. | Remove two moving npm inputs immediately while preserving an explicit evidence boundary around installer channels that still lack locally recoverable revisions or checksums. |
| 2026-09-11 | Completed rebuilt-image validation for Issue 1. | Installed entrypoint, helper, supervisor, and CUDA wrapper hashes match staged source and are executable. Cortex retained `{:id "unix-transport-20260911", :value 9264}` across separate calls; its JVM saw only `lo` with `127.0.0.1`; status retained the same PID; both acceptance cycles stopped cleanly and removed the Unix socket. | Confirm the security boundary in the actual application runtime rather than relying only on source and fake-nREPL fixtures. |
| 2026-09-11 | Implemented Issue 1's networkless persistent REPL and completed Issues 47 and 48. | The full approved-boundary no-image suite passes inside this container: 13 focused transport tests plus a Bubblewrap lifecycle proving only `lo`, mode-`0600` Unix transport, successful nREPL evaluation, confirmed stop, and socket removal; CUDA and ACP regression fixtures also pass. | Close the REPL network-approval bypass while preserving managed local evaluation and repair the two fixtures that previously hid later validation. |
| 2026-09-11 | Validated pathname Unix IPC as the Issue 1 isolation building block, completed Issue 41's production relay validation, and opened smoke-test Issues 47 and 48. | Cortex retained state and served a pathname Unix socket to a separate client; a networkless Bubblewrap server returned `ENETUNREACH` for outbound IP but exchanged `pong:ping`; ACP delayed failure returned 1 without starting ACP, while the positive real-`socat` path returned `pong:relay` and terminated on signal 15. | Confirm the proposed local-IPC boundary with runtime evidence while recording newly exposed test defects. |
| 2026-09-11 | Made Issue 23 optional and deferred until substantial Dockerfile growth or demonstrated profile drift. | Source audit found roughly 200 duplicated lines but no unintended behavioral drift; package pruning remains a development-environment support decision. | The current maintainability risk does not justify architecture or compatibility work. |
| 2026-09-11 | Explicitly deferred the remaining `codex-push`/`codex-pull` work and ordered snapshot items after the general backlog. | Planning/status review identified Issues 4, 20, 21, 37, and 44 as the remaining snapshot-specific work; completed snapshot items retain their evidence. | Focus current effort on higher-priority general repository work. |
| 2026-09-11 | Reassessed and dismissed Issue 15's broad `/tmp` project-path claim; retained an optional exact-`/tmp` guard. | Source review confirms `/run` is already denied, `/tmp/PROJECT` is only a child bind destination, and `.local-fixtures/cortex` is ignored and absent from tracked files. | Separate Git tracking of disposable checkouts from container mount destinations and avoid speculative restrictions. |
| 2026-09-11 | Completed Issues 5, 18, and 19 with strict push parsing, requested-generation force recovery, and shared snapshot validation. | `SNAPSHOT-5-18-19-20260911` passed argument non-mutation, incomplete-head recovery, forward/divergence behavior, and shared malformed-metadata rejection with exit 0. | Restore recovery behavior and prevent parser mistakes or validation drift from mutating synchronized state. |
| 2026-09-11 | Completed Issue 32's stable decomposition across launcher, tests, and documentation. | Doctor extraction is byte-for-byte equivalent; installed-companion probes, standalone snapshot tests, shell syntax, links, and whitespace checks pass. README is 217 lines shorter and the launcher is 344 lines shorter. | Reduce review surface while preserving public commands and security behavior. |
| 2026-09-11 | Completed Issue 36 by documenting the lock-free snapshot-listing contract and transient `INVALID` results. | Focused source assertions require both the lock-free contract and transient-result warning. | Make recovery output interpretable while a sync provider is still transferring files. |
| 2026-09-11 | Completed Issue 35 by replacing Seafile-specific push success and handoff messages with provider-neutral wording. | Shell syntax passes; source assertions require the new provider-neutral guidance and reject the old output. | Keep operator output accurate when `CODEX_SYNC_DIR` uses Dropbox, Syncthing, or another provider. |
| 2026-09-11 | Verified the current rebuilt CUDA image, completed Issues 7, 8, 25, 26, and 33, and renamed the exact-image launcher override to `--image-version`. | Installed-file hashes and modes match; live `/dev`, `setns`, ordinary-thread, and CUDA-304 diagnostic probes pass; focused parser checks reject the old option and invalid/duplicate versions; linked-worktree regression fixture added. | Close the remaining rebuilt-image checks, use clearer version terminology, and document the safe worktree workaround without widening repository mounts. |
| 2026-09-11 | Added and completed Issue 46 and revised Issue 43 after the post-commit rebuild retained stale `latest`. | Focused disposable probes confirm one-session tag selection plus duplicate/invalid/session-list rejection; fake builds confirm derived and explicit dirty image inputs receive `-dirty`, visibly update `latest` when requested, and leave unrelated working files out of the dirty decision. | Preserve convenient `latest` publication while making dirty builds identifiable and exact immutable selection available. |
| 2026-09-11 | Completed Issues 13 and 14 by making handoff-lock waits visible and moving shared acquisition before session-state reads. | Focused concurrent fixture held the exclusive lock, observed the waiting message and live child, inserted a session row, released the lock, and confirmed the later listing saw that row. | Make contention diagnosable and close the session-resolution replacement race. |
| 2026-09-11 | Completed the approved elevated Cortex GPU validation tranche. | CUDA foundation: 9 tests/18 assertions; common GPU: 693 tests/2,823 assertions; GPU integration: 108 tests/5,645 assertions. All three completed with zero failures; the latter two emitted only the known Meander reflection warning. | Confirm actual native and application GPU behavior beyond device visibility while preserving the command-sandbox boundary. |
| 2026-09-11 | Promoted and completed Issue 45 with `run-codex PROJECT --set KEY VALUE` syntax for `profile` and `clojure-mcp`; removed the obsolete leading option forms after confirmation that no external callers exist. | Focused disposable registry probes pass for both setters, companion-value preservation, removed-form rejection, unknown-key rejection, and missing-value rejection. | Remove an irritating project/value ordering inconsistency and keep one configuration interface. |
| 2026-09-11 | Retested CUDA in the second resumed container. | Sandboxed R1: 9 tests, 13 assertions, 5 `cudaHostAlloc` errors. Approved elevated R2: 9 tests, 18 assertions, 0 failures/errors. GPU and native CUDA are confirmed; the installed image still predates pending wrapper/mode changes. | Separate working GPU/application behavior from the command sandbox and stale-image tag. |
| 2026-09-11 | Completed Issue 34 by correcting the obsolete `run-codex.sh` name in the snapshot lock comment. | Literal search finds the old name only in tracking descriptions of this completed item. | Keep operator-facing source comments accurate. |
| 2026-09-11 | Completed Issues 29, 30, and 31 by aligning managed-helper/reuse rules, warning about unmanaged nREPL, and documenting the canonical host-suite coverage boundary. | TOML parsing and whitespace checks pass; wording was reconciled across the probe config, `AGENTS.md`, `CONTRIBUTING.md`, and Clojure-agent guide. | Keep workflow and validation guidance consistent without overstating sandbox or image evidence. |
| 2026-09-11 | Implemented the source-level portion of Issue 25 with waited bounded diagnostic readers and conventional hint modes. | Focused fake-Bubblewrap checks pass for failure status, hint matching, truthy/off modes, and waited capture; the host-suite fallback fixture and real CUDA remain pending. | Make diagnostics deterministic without claiming unavailable GPU evidence. |
| 2026-09-11 | Completed Issue 28 by reducing the Clojure-LSP allowlist. | Source assertion rejects `execute_command` and `suggest_fixes`; existing policy retains user review for scoped write tools. | Remove broad or semantically ambiguous capabilities from the MCP boundary. |
| 2026-09-11 | Completed Issue 24 by restricting workflow-source overrides to bounded `/tmp` test fixtures and fixing the production supervisor path to the image copy. | Installer lifecycle checks include rejection-before-write for an unflagged override. | Keep managed workflow replacement out of the production configuration surface. |
| 2026-09-11 | Completed Issue 39 by restoring `nullglob` after each scoped expansion in the launcher and snapshot commands. | Shell syntax and paired enable/restore source assertions pass. | Prevent hidden shell-option state from affecting later operations. |
| 2026-09-11 | Completed Issue 38 by expanding launcher help to cover all supported user-facing environment variables. | Help-output assertions cover each formerly omitted variable. | Make configuration discoverable without source inspection. |
| 2026-09-11 | Completed Issue 26 by denying `setns` in the post-Bubblewrap child filter. | Focused compiled-filter checks cover ordinary commands, runtime threads, user-namespace denial, and `setns` denial. | Apply least privilege after namespace setup without constraining Bubblewrap itself. |
| 2026-09-11 | Implemented Issue 41 by replacing the fixed ACP relay sleep with bounded listener polling and parent-death cleanup. | Source checks pass; the delayed-failure fixture was added but cannot create its Unix socket in this sandbox, so host validation remains pending. | Detect delayed bind failures and keep relay lifetime coupled to ACP. |
| 2026-09-11 | Completed Issue 11 by resolving the host primary group with `id -g`. | Shell syntax and focused source regression assertions pass. | Avoid relying on supplementary-group array ordering for container ownership. |
| 2026-09-11 | Added Issue 45 for a project-first, Git-config-style `--set KEY VALUE` command while preserving compatibility aliases. | Current `--set-profile PROJECT VALUE` and `--set-clojure-mcp PROJECT VALUE` usage/source confirmed. | Make persistent project configuration predictable and extensible. |
| 2026-09-11 | Retested the resumed container and Cortex CUDA suite. Closed Issues 10 and 42; kept Issues 7 and 8 blocked because the session is an older generic image. | Live kernel checks pass; packaged worker script is `0644`; CUDA wrapper/devices/libraries are absent; Cortex CUDA test exits before application assertions with missing `jnicudart`. | Separate verified host enforcement from invalid CUDA/image-mode evidence. |
| 2026-09-11 | Completed Issue 17 by making the mandatory Git-provenance contract explicit instead of permitting provenance-free tarball builds. | Focused non-Git build fixture rejects explicit slug/version with the documented provenance error. | Preserve the repository invariant that every image has Git-derived revision and source metadata. |
| 2026-09-11 | Finished the first tranche: implemented Issues 2, 7, 8, 10, 12, 16, 27, 42, and 43; completed Issues 9 and 40; deferred Issues 1 and 22. | Focused disposable security probes pass for Issues 2, 7, 12, 16, 27, and 43; syntax/static/whitespace checks pass. The host suite passed 18 Python tests before an nREPL startup failure later diagnosed as this sandbox prohibiting `AF_INET`. Issues 7 and 8 still await a current image. | Preserve the earlier handoff point and its later diagnostic correction. |
| 2026-09-11 | Completed Issue 40 by adding `.local-fixtures/` to `.gitignore`. | `git check-ignore .local-fixtures/probe` passes. | Prevent disposable fixtures from appearing as commit candidates on every checkout. |
| 2026-09-11 | Deferred Issue 22 without changing dependency versions. | No network or gated-resource lookup was performed. | Concrete pins are safer than guessed versions; follow the user's no-fetch direction. |
| 2026-09-11 | Completed Issue 9 by correcting the image/runtime identity description in `AGENTS.md`. | Wording checked against the existing README identity description. | Remove a misleading contributor instruction without waiting on code changes. |
| 2026-09-11 | Started Issue 8 by aligning executable workflow-script modes in both Dockerfiles. | Source assertions pass; rebuilt-image mode validation remains pending. | Take a contained disjoint fix while Tier 1 test ownership is active elsewhere. |
| 2026-09-11 | Deferred Issue 1 after architecture review. A container-wide `--network none` change was rejected because it would disable approved network operations; safe isolation requires Unix-socket nREPL plumbing or an equivalent private namespace. | Cortex exploit probe remains valid; no Issue 1 source change was made. | Follow the user's direction to skip work that would stall the remaining contained fixes. |
| 2026-09-11 | Started Tier 1 implementation for Issues 1, 2, 7, 10, 12, 16, 27, 42, and 43. Issue 22 remains queued behind the contained fixes because pin selection needs an explicit dependency-update policy. | Work boundaries assigned; validation pending. | Close confirmed security bypasses before lower-priority sync and maintenance work. |
| 2026-09-11 | Established plan and status baseline from `review.md`, source audit, disposable host fixtures, and the Cortex persistent-REPL probe. No runtime fixes applied yet. | Targeted probe run completed; Cortex REPL was stopped with confirmed termination; full host suite was incomplete because the local nREPL supervisor failed before image checks. | Preserve a verified starting point before implementation. |
